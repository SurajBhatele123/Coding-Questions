#include <iostream>

using namespace std;

int main()
{
       int a[]={1,2,3,4,5};
       int max=a[0];
       int min=a[0];
       for(auto x:a){
           if(x>max)
             max=x;
            else
            min=x;
       }
        cout<<max<<"  "<<min;
    
       
    return 0;
}

